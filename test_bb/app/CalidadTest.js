define([

	'calidad'

], function( Calidad ){
	
	QUnit.module('Módulo Calidad', function(hooks){

		hooks.beforeEach(function(assert){


		});
	});

	QUnit.test('Funcion establecer_entorno', function(assert){

		assert.ok( true, 'test de prueba');
	});
});